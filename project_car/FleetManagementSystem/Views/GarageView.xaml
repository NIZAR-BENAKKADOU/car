<UserControl x:Class="FleetManagementSystem.Views.GarageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:FleetManagementSystem.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodels:GarageViewModel}"
             Background="Transparent">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Gestion des Garages" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="Pilotage des localisations et informations des garages partenaires." Style="{StaticResource PageSubtitle}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource FormCardBorder}" Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,14" MaxWidth="460" HorizontalAlignment="Left">
                    <TextBlock Text="Nom du Garage" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource FormTextBox}"
                             Text="{Binding SelectedGarage.Nom, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,8">
                    <TextBlock Text="Latitude" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource FormTextBox}"
                             Text="{Binding SelectedGarage.Latitude, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,8">
                    <TextBlock Text="Longitude" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource FormTextBox}"
                             Text="{Binding SelectedGarage.Longitude, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Nouveau" Command="{Binding ClearCommand}" Style="{StaticResource NeutralActionButton}" Width="112" Margin="0,0,10,0"/>
                    <Button Content="Ajouter" Command="{Binding AddCommand}" Style="{StaticResource SuccessActionButton}" Width="112" Margin="0,0,10,0"/>
                    <Button Content="Modifier" Command="{Binding UpdateCommand}" Style="{StaticResource PrimaryActionButton}" Width="112" Margin="0,0,10,0"/>
                    <Button Content="Supprimer" Command="{Binding DeleteCommand}" Style="{StaticResource DangerActionButton}" Width="112"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource DataCardBorder}">
            <DataGrid ItemsSource="{Binding Garages}" SelectedItem="{Binding SelectedGarage}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                    <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                    <DataGridTextColumn Header="Latitude" Binding="{Binding Latitude}" Width="180"/>
                    <DataGridTextColumn Header="Longitude" Binding="{Binding Longitude}" Width="180"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
