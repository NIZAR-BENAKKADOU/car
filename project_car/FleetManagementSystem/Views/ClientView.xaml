<UserControl x:Class="FleetManagementSystem.Views.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:FleetManagementSystem.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodels:ClientViewModel}"
             Background="Transparent">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Gestion des Clients" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="Administration des profils clients et informations de contact." Style="{StaticResource PageSubtitle}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource FormCardBorder}" Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,14">
                    <TextBlock Text="CIN" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource FormTextBox}"
                             Text="{Binding SelectedClient.Cin, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,14">
                    <TextBlock Text="Email" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource FormTextBox}"
                             Text="{Binding SelectedClient.Email, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,14">
                    <TextBlock Text="Nom" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource FormTextBox}"
                             Text="{Binding SelectedClient.Nom, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,14">
                    <TextBlock Text="Prénom" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource FormTextBox}"
                             Text="{Binding SelectedClient.Prenom, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,8" MaxWidth="360" HorizontalAlignment="Left">
                    <TextBlock Text="Téléphone" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource FormTextBox}"
                             Text="{Binding SelectedClient.Telephone, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Nouveau" Command="{Binding ClearCommand}" Style="{StaticResource NeutralActionButton}" Width="112" Margin="0,0,10,0"/>
                    <Button Content="Ajouter" Command="{Binding AddCommand}" Style="{StaticResource SuccessActionButton}" Width="112" Margin="0,0,10,0"/>
                    <Button Content="Modifier" Command="{Binding UpdateCommand}" Style="{StaticResource PrimaryActionButton}" Width="112" Margin="0,0,10,0"/>
                    <Button Content="Supprimer" Command="{Binding DeleteCommand}" Style="{StaticResource DangerActionButton}" Width="112"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource DataCardBorder}">
            <DataGrid ItemsSource="{Binding Clients}" SelectedItem="{Binding SelectedClient}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="CIN" Binding="{Binding Cin}" Width="140"/>
                    <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                    <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="230"/>
                    <DataGridTextColumn Header="Téléphone" Binding="{Binding Telephone}" Width="145"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
